# Better Auth
BETTER_AUTH_SECRET=   # Generate with: openssl rand -base64 32
BETTER_AUTH_URL=http://localhost:3000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/your-db

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# =============================================================================
# Environment Detection (lib/env.ts)
# These variables control application behavior via ENV flags and helper functions
# =============================================================================

# NODE_ENV - Auto-set by Next.js (do not set manually)
# Values: "development" | "test" | "production"
# Used by: ENV.isDev, ENV.isProd, ENV.isTest, isDevelopment(), isProduction(), isTestMode()

# NEXT_PHASE - Auto-set by Next.js during build (do not set manually)
# Value: "phase-production-build" during `pnpm build`
# Used by: ENV.isBuild, isBuildTime()

# NEXT_PUBLIC_PLAYWRIGHT_TEST - Set for Playwright e2e tests
# Value: "true" to enable test mode
# Used by: ENV.isTest, isTestMode()
# NEXT_PUBLIC_PLAYWRIGHT_TEST=true